{
    "version": 2,
    "redirects": [
        { "source": "/(.*)", "destination": "/server.js" }
      ],
    "builds":[
        { "src": "*.js", "use": "@vercel/node" }
    ],
    "routes":[
        { "src": "/(.*)", "dest": "/server.js" }
    ]
}